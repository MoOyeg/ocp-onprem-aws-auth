---
# Ansible collections required by the ocp-onprem-aws-auth playbooks.
# Installed into the runner image at build time (see Containerfile).
collections:
  - name: kubernetes.core
    version: ">=3.0.0"
  - name: amazon.aws
    version: ">=7.0.0"
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.utils
    version: ">=2.10.0"
